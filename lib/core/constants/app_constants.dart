class AppConstants {
  const AppConstants._();

  static const appName = 'ReFind UTeM';
  static const appTagline = 'Lost something on campus? Start here.';
  static const appDescription =
      'A trusted lost and found space for the UTeM Family.';
}
